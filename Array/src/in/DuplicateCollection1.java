package in;

import java.util.LinkedHashSet;
import java.util.Set;

public class DuplicateCollection1 {
public static void main(String[] args) {	

/*
 * import java.util.Arrays;
import java.util.LinkedHashSet;

public class RemoveDuplicatesFromArray {
    public static void main(String[] args) {
        // Original array with duplicates
        Integer[] array = {1, 2, 3, 4, 2, 5, 6, 1};

        // Convert array to a LinkedHashSet to remove duplicates while maintaining order
        LinkedHashSet<Integer> set = new LinkedHashSet<>(Arrays.asList(array));

        // Convert the set back to an array
        Integer[] uniqueArray = set.toArray(new Integer[0]);

        // Print the unique array
        System.out.println("Array with duplicates removed: " + Arrays.toString(uniqueArray));
    }
}

 */

}
}
